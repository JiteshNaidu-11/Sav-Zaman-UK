/**
 * Demo re-export — the live catalogue is built in `src/data/dummyStoreProperties.ts`
 * and exported from `src/data/properties.ts`. Use in app code:
 *
 *   import { properties } from "@/data/properties";
 */
export { properties, type Property } from "../src/data/properties";
